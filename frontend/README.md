Minimal Next.js frontend for Portfolio Manager.

Run:

1. Install dependencies: npm install
2. Start dev server: npm run dev

Assumes backend is running at http://localhost:8080 and exposes /api endpoints.

This frontend is intentionally minimal and uses SWR for data fetching.
